/**
 * Adapter boundary: Portfolio Blueprint ↔ Puck data model.
 *
 * Blueprint remains the source of truth.
 * Puck is a replaceable editor implementation — do not leak
 * Puck types into Blueprint types or application domain logic.
 */
export {};
