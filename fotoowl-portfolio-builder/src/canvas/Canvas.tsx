/**
 * Interactive portfolio Canvas — mounts the editor surface
 * and renders Blueprint-backed sections.
 *
 * POC target: Blueprint → Registry → Puck Adapter → Canvas
 */
export function Canvas() {
  return null;
}
